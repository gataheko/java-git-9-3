# java-git-9-3
Demonstrates Java exception propagation through the call stack. Shows how checked exceptions (IOException) propagate from method3() through method2() and method1() using throws clauses, ultimately being caught in main(). Includes examples of getMessage(), toString(), and printStackTrace() for error reporting.
